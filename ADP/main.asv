clear;


xm = 0; ym =0; vm = 600; delt_m = 30 / 180 * pi;

xt = 2500; yt = 0; vt = 400;  delt_t = 125/180 * pi;

q = atan2(yt-ym,xt-xm);

W = [50;50;50;50]; % 初始的W

r = sqrt((xm-xt)^2 + (ym - yt)^2);

h = 0.001;
N = 1000 * 10;

% 存储信息用的
R = zeros(1,N);
Q = zeros(1,N);
XM = zeros(1,N);
YM = zeros(1,N);

